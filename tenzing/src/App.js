import React from 'react';
import './App.css';

function App() {
 const [strike, setStrike]=useState(0);
 const [ball, setBall]=useState(0);

 const addStrikes= currentStrike =>{
   if(currentStrike >=2){
     setStrikeCount(0)
     setBallCount(0)
    }else{
      setStrikeCount(strikeCount+1)
    }
 }

 const addBalls = currentElements

  return (
    
  );
}

export default App;
